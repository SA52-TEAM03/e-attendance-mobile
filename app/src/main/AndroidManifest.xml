<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="sa52.team03.adproject">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EAttendanceMobile">

        <meta-data
            android:name="com.google.android.actions"
            android:resource="@xml/network_security_config" />

        <activity android:name=".FaceAuthenticationActivity"/>
        <activity android:name=".QRCodeActivity" />
        <activity android:name=".SubmitLeaveActivity" />
        <activity android:name=".CheckAttendanceActivity" />
        <activity android:name=".ClassScheduleActivity" />
        <activity android:name=".AttendanceSuccessActivity" />
        <activity android:name=".AttendanceFailureActivity" />
        <activity android:name=".StudentMainActivity" />
        <activity android:name=".LogInActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>